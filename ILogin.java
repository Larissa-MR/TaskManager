import java.io.File;
import java.nio.file.*;
import java.util.Scanner;
import java.util.stream.Stream;

public class ILogin {

	
	
	public ILogin() {
		
		
	
	File file = new java.io.File("myfile.txt");
	long count=0;
	try (Stream<Path> employeeFiles = employeeFiles.list(Paths.get("employees"))) {
	    count = employeeFiles.count();
	}
	for(int i; i<count; )

	

	rightEmployee = SystemManager.loginIsValid(empList, inpUser, inpPass);
	}
}
